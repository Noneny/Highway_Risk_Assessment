# Highway Risk Assessment 项目升级计划

## 一、升级目标

在不改写现有点、线、网三级风险计算实现的前提下，将脚本式程序升级为可被 Java 运营系统稳定调用、可容器化部署的模型服务。

目标调用链路：

```text
运营系统 → Spring Boot Service → HTTP → FastAPI → Python评估应用模块
                                              ├─ MySQL输入/输出
                                              └─ 点 → 线 → 网模型
```

现有全流程约需 20 分钟，并且多个阶段会读写同一批 Excel 中间文件，因此采用异步任务接口和单任务串行执行策略。Java 提交任务后立即获得任务 ID，再轮询任务状态，不长时间占用 HTTP 连接。

## 二、实施阶段

| 阶段 | 改进内容 | 验收标准 | 状态 |
|---|---|---|---|
| 1. 应用模块抽取 | 将现有主流程封装为 `RiskAssessmentRunner`，统一任务参数、阶段异常和产物信息 | 命令行流程不受影响；调用一个接口即可执行完整流程 | 已完成 |
| 2. FastAPI模型接口 | 新增健康检查、任务提交、任务状态查询接口 | 返回 202 和任务 ID；可查询四种任务状态及输出文件 | 已完成 |
| 3. Java调用层 | 新增 Spring Boot 工程，在 Service 层通过 HTTP 调用 Python | Java 对外提供提交、查询接口；Python地址可配置 | 已完成 |
| 4. 容器化部署 | 分别构建 Python、Spring Boot 镜像，由 Compose 编排 | 两个进程可独立启动、健康检查并通过服务名通信 | 已完成 |
| 5. 配置与安全 | 数据库连接支持环境变量覆盖，镜像排除真实数据与数据库INI | 镜像中不固化企业数据和数据库密码 | 已完成 |
| 6. 自动化验证 | 增加任务状态机、FastAPI接口和Java调用接口测试 | 测试覆盖成功、失败、非法参数及不存在任务 | 已完成 |

## 三、模块与接口设计

### 1. Python评估应用模块

外部只需要了解 `AssessmentCommand` 和 `RiskAssessmentRunner.run()`。点、线、网的执行顺序、文件传递和结果收集均隐藏在模块内部。

任务参数：

- `prepare_input`：是否执行 ETC 合并及 MySQL 数据导出。
- `update_config`：是否从配置库同步 INI。
- `compare`：是否执行两期对比分析。
- `recalculate`：是否重新运行点、线、网评估。

### 2. FastAPI接口

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/health` | 进程健康检查及忙碌状态 |
| `POST` | `/api/v1/assessments` | 提交评估任务 |
| `GET` | `/api/v1/assessments/{task_id}` | 查询任务阶段、结果和错误 |

任务状态：`QUEUED`、`RUNNING`、`SUCCEEDED`、`FAILED`。

### 3. Spring Boot调用模块

- `RiskAssessmentClient`：Java 业务层依赖的调用接口。
- `FastApiRiskAssessmentClient`：生产环境 HTTP 适配器。
- `RiskAssessmentService`：运营系统 Service 层调用入口。
- `RiskAssessmentController`：示例 REST 控制器。

Java 调用超时仅覆盖“提交任务”和“查询状态”这两个短请求，不等待 20 分钟模型计算完成。

## 四、后续生产化建议

当前版本适用于单实例、单任务串行运行。进入多实例或高并发生产环境前，建议继续完成：

1. 将内存任务状态迁移到 MySQL 或 Redis，支持服务重启后恢复查询。
2. 将共享 Excel 中间文件改为按任务 ID 隔离的工作目录。
3. 引入任务队列，并根据服务器资源设置并发上限。
4. 为企业运营系统增加认证、调用审计和输出文件下载权限控制。
5. 建立固定样本数据集，对点、线、网计算结果执行回归测试。
