# 模型接口与 Docker 部署说明

## 一、本地启动

### Python模型接口

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn src.api.app:app --host 0.0.0.0 --port 8000 --workers 1
```

接口文档地址：`http://localhost:8000/docs`。

> 必须保持一个 Uvicorn worker。任务状态当前保存在进程内，评估流程还会读写共享中间文件。

### Spring Boot调用层

```bash
cd java-service
mvn test
mvn spring-boot:run
```

默认连接 `http://localhost:8000`，可通过 `RISK_API_BASE_URL` 修改。

## 二、调用示例

通过 Java 服务提交任务：

```bash
curl -X POST http://localhost:8080/api/risk-assessments \
  -H 'Content-Type: application/json' \
  -d '{
    "prepare_input": true,
    "update_config": false,
    "compare": false,
    "recalculate": true
  }'
```

响应示例：

```json
{
  "task_id": "087b1a6a-6a13-45d8-a647-c583f784b153",
  "status": "QUEUED",
  "phase": "QUEUED",
  "created_at": "2026-08-11T08:00:00+00:00",
  "started_at": null,
  "finished_at": null,
  "result": null,
  "error": null
}
```

查询任务：

```bash
curl http://localhost:8080/api/risk-assessments/087b1a6a-6a13-45d8-a647-c583f784b153
```

任务成功后，`result.artifacts` 会返回本次生成的点、线、网结果文件相对路径、文件大小及更新时间。

## 三、Docker Compose部署

1. 复制环境变量模板并填写企业数据库连接：

   ```bash
   cp .env.example .env
   ```

2. 确认以下宿主机目录：

   - `config/`：模型参数配置。
   - `data/`：模型输入、临时数据及输出。
   - `log/`：运行日志。
   - `.env` 中的 `ETC_DATA_DIR`：双月 ETC 原始 Excel 目录。

3. 构建并启动：

   ```bash
   docker compose up -d --build
   ```

4. 检查服务：

   ```bash
   curl http://localhost:8000/health
   curl http://localhost:8080/actuator/health
   ```

## 四、环境变量

| 变量 | 说明 |
|---|---|
| `RISK_BELONG_DATE` | 当前双月评估周期的起始日期 |
| `RISK_INPUT_DB_*` | 原始气象、事故等输入数据库连接 |
| `RISK_OUTPUT_DB_*` | 点、线、网结果数据库连接 |
| `RISK_CONFIG_DB_*` | 配置同步数据库连接 |
| `RISK_API_BASE_URL` | Spring Boot访问Python接口的地址 |
| `ETC_DATA_DIR` | 宿主机ETC原始文件目录 |

容器构建上下文通过 `.dockerignore` 排除了真实 `data/`、日志以及包含密码的数据库 INI。运行时优先读取环境变量；未提供环境变量时才使用挂载的 INI 配置。

## 五、常见任务参数

- 完整评估：`prepare_input=true, recalculate=true`。
- 使用已有输入文件：`prepare_input=false, recalculate=true`。
- 完整评估并对比：`prepare_input=true, recalculate=true, compare=true`。
- 仅使用已有结果做对比：`prepare_input=false, recalculate=false, compare=true`。
