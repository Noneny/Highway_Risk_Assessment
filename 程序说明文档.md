# 高速公路路网通行风险评估系统 — 程序说明文档

> 版本: 2.0.0 (企业集成版) | 重庆山区高速公路专项

> 3.0 工程化升级已新增 FastAPI 异步模型接口、Spring Boot 调用层与 Docker Compose 部署。调用方式和环境变量见《接口与部署说明.md》，实施范围见《项目升级计划.md》。

---

## 一、项目概述

本系统对**重庆山区高速公路**进行多层级通行风险评估，覆盖"**结构点 → 路段 → 路网**"三个维度，支持**跨周期对比分析**，辅助运营管理单位识别、跟踪和处置交通风险。

### 核心能力

| 能力 | 说明 |
|------|------|
| 结构点风险评估 | 评估桥梁、隧道、边坡、门架等关键结构点通行风险 |
| 路段风险评估 | 按路段（路线+方向）汇总评估通行风险 |
| 路网风险评估 | 按运营公司/示范路网维度评估整体路网风险 |
| 跨周期对比 | 对比相邻两期的风险变化，标注"风险上行/当期高风险/持续高风险" |
| 风险归因分析 | 自动识别风险来源（技术状况、灾害、交通流、事故等） |

---

## 二、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                    main.py (总控调度器)                        │
├──────────┬──────────┬──────────┬──────────┬─────────────────┤
│ 配置更新  │ 数据准备  │ 点风险评估 │ 线风险评估 │ 网风险评估       │
│ Config   │ Input    │ Point    │ Line     │ Net             │
│ Update   │ (阶段 0)  │ (阶段 1)  │ (阶段 2)  │ (阶段 3)         │
├──────────┴──────────┴──────────┴──────────┴─────────────────┤
│                    对比分析模块 Compare (阶段 4)               │
└──────────────────────────────────────────────────────────────┘
```

### 分层设计

```
main.py                     # 总控入口，命令行参数解析，阶段编排
  └── src/
      ├── db_config.py      # 统一数据库配置读取
      ├── input/            # 阶段 0: 数据准备（ETC数据合并、数据库导出）
      ├── point_risk/       # 阶段 1: 结构点风险评估
      ├── line_risk/        # 阶段 2: 路段风险评估
      ├── net_risk/         # 阶段 3: 路网风险评估
      ├── compare/          # 阶段 4: 两期对比分析
      └── config_update/    # 前置: 从数据库同步配置文件
```

每个子模块统一采用以下内部结构：
```
{module}/
  ├── {module}_main.py               # 模块独立入口（可单独运行）
  ├── config/config_manager.py       # 模块配置管理器
  ├── data_processing/               # 数据处理层
  │   ├── base_processor.py          # 抽象基类（共享工具函数）
  │   └── *_processor.py             # 各类数据处理器
  ├── models/*_models.py             # 数据模型（dataclass）
  ├── risk_calculation/              # 风险计算层
  │   └── risk_calculator.py
  ├── database/database_connector.py # 数据库读写
  └── workflow/*_workflow.py         # 工作流编排（模块主入口）
```

---

## 三、模块详细说明

### 3.1 配置更新模块 (`src/config_update/`)

**用途**: 在风险评估执行前，从 MySQL 数据库 `freeway_risk_config` 同步最新配置到本地 `config/*.ini` 文件。

**工作流程**:
1. 连接 MySQL `freeway_risk_config` 数据库
2. 读取 6 张表（compare, input_db, line_risk, net_risk, output_db, point_risk）
3. 每张表结构: `section, config_key, config_value`
4. 更新对应的 `config/*.ini` 文件（保留原有注释）

**触发方式**: `python main.py --config-update`

---

### 3.2 输入数据准备模块 (`src/input/`)

**用途**: 为后续风险评估准备输入数据文件。

**工作流程**:
1. **ETC 数据合并** (`excel_tocsv.py`): 将 `../ETC_Data/` 目录下双月门架的多个 Excel 文件合并为单一 CSV
2. **数据库导出** (`database_exporter.py`): 从 MySQL `freeway_risk_input` 导出三张表：
   - `point_alert_{suffix}` → JSON 预警文件（按结构点级别）
   - `district_alert_{suffix}` → `气象预警.xlsx`（按区县级别）
   - `accident_{suffix}` → `交通事故.xlsx`

其中 `{suffix}` 格式为 `{yy}_{m}_{yy}_{m+1}`，如 `25_12_26_1` 表示 2025年12月至2026年1月。

---

### 3.3 结构点风险评估模块 (`src/point_risk/`)

**评估对象**: 桥梁、隧道、边坡等关键结构点。

**五步工作流**:

| 步骤 | 处理器 | 说明 |
|------|--------|------|
| 1. 结构数据处理 | `structure_processor.py` | 加载监测基础信息，映射综合等级→基础风险值 |
| 2. 气象数据处理 | `weather_processor.py` | 5km半径内气象预警天数统计 → 动态风险系数 |
| 3. 交通数据处理 | `traffic_processor.py` | ETC门架数据分析（拥挤度、大车占比、速度离散度） |
| 4. 路段标定 | `roadname_processor.py` | 门架编码→公司名称/路段名称补充 |
| 5. 风险计算 | `risk_calculator.py` | 汇总计算总风险值、风险等级、风险归因 |

**基础风险值映射**:
| 综合等级 | 基础风险值 |
|----------|-----------|
| 一级（最高风险） | 83 |
| 二级 | 72 |
| 三级 | 55 |
| 四级（最低风险） | 48 |

**气象动态风险系数**（5km半径范围内预警天数）:
| 预警天数 | 系数 |
|----------|------|
| 0 天 | 1.00 |
| 1-10 天 | 1.05 |
| 1-20 天 | 1.08 |
| >20 天 | 1.12 |

**交通动态风险**: 评估三个维度——拥挤度（高峰流量/通行能力）、交通组成（大车占比）、速度离散度（变异系数），每维度取 1.00/1.05/1.10 三档。

**最终公式**:
```
总风险值 = 基础风险值 × 动态风险叠加 × 专项管控折减(0.98)
```

**风险等级划分**:
| 总风险值 | 等级 |
|----------|------|
| < 60 | 低风险 |
| 60-80 | 一般风险 |
| 80-100 | 较高风险 |
| ≥ 100 | 高风险 |

---

### 3.4 路段风险评估模块 (`src/line_risk/`)

**评估对象**: 按路段（路线+方向）计算通行风险。

**六阶段工作流**:

| 阶段 | 处理器 | 说明 |
|------|--------|------|
| 1. 数据加载 | — | 读入结构点评估结果、评价模板、门架信息、气象/事故数据 |
| 2. 基础风险（静态） | `base_risk_processor.py` | 结构物评分 + 气象基础分 + 平纵线形风险 → 基础风险F |
| 3. 动态风险 | `dynamic_risk_processor.py` | ETC交通分析 + 路段气象预警 → 动态系数 |
| 4. 附加风险 | `extra_risk_processor.py` | 事故频次评分 + 旅游公路系数(1.05) |
| 5. 综合评估 | `risk_calculator.py` | 总评 = 基础F × 动态系数 × 附加系数 |
| 6. 结果输出 | — | 保存 Excel + 写 MySQL |

**基础风险 F 组成**（三项归一化后满分各16，合计上限100）:
- F1: 结构物归一化评分
- F2: 气象基础分（结冰/大雾月数统计）
- F3: 平纵线形风险

**最终公式**:
```
路段风险总评 = 基础风险_F总值 × 动态风险_总系数 × 附加风险_总系数
```
结果上限设为 99.99，超过即标记为一级（高风险）。

---

### 3.5 路网风险评估模块 (`src/net_risk/`)

**评估对象**: 按运营公司（东南/东北/渝东）和示范路网维度评估。

**九步工作流**:

| 步骤 | 说明 |
|------|------|
| 1. 加载交通流量数据 | `traffic_flow_processor.py` |
| 2. 加载路段风险数据 | `road_risk_processor.py` |
| 3. 加载事件数据 | `event_data_processor.py` |
| 4. 数据合并 | 按路段名称合并流量、风险、基础信息、路由 |
| 5. 基础风险计算 | R(路段风险) + C(连通度) + B(路网密度) → F1+F2+F3 |
| 6. 动态系数计算 | 饱和度系数 × 均衡性系数 |
| 7. 附加系数计算 | 30分钟到达率系数 × 1小时恢复通行率系数 |
| 8. 路网风险计算 | 基础风险 × 动态系数 × 附加系数 |
| 9. 风险归因分析 | 各因子贡献度百分比 |

**基础风险三元组**:
- **R (路段通行风险综合值)**: 按交通流量加权平均的各路段风险值
- **C (路网连通度通行风险值)**: `100 × (1 - C'/2)`，C' = 相邻路段数/节点数
- **B (路网密度通行风险值)**: `(参考密度 - 实际密度)/参考密度 × 100`，参考密度 = 5.12

**F1-F2-F3 评分逻辑**:
```
F1 = max(R, C, B)
F2 = (100 - F1) × second_rank / 100
F3 = (100 - F1 - F2) × third_rank / 100
基础风险总值 = F1 + F2 + F3
```

**动态调节系数 = 饱和度系数 × 均衡性系数**:

**饱和度系数**（加权平均饱和度 S）:
| S 范围 | 系数 |
|--------|------|
| < 0.6 | 1.00 |
| < 0.8 | 1.00 |
| < 1.0 | 1.10 |
| ≥ 1.0 | 1.20 |

饱和度计算公式: `S = Σ(流量×长度) / Σ(设计通行能力×长度)`（按路段长度加权）

**均衡性系数**（E = 1 - σ/均值，反映各路段饱和度离散程度）:
| E 范围 | 系数 |
|--------|------|
| ≥ 0.6 | 1.00 |
| ≥ 0.5 | 1.00 |
| ≥ 0.4 | 1.05 |
| ≥ 0.3 | 1.10 |
| < 0.3 | 1.20 |

**附加风险修正系数 = 到达率系数 × 恢复率系数**:
| 指标 | 达标(≥0.9) | 不达标(<0.9) |
|------|-----------|-------------|
| 30分钟到达率 | 0.95 | 1.02 |
| 1小时恢复通行率 | 0.95 | 1.02 |

**最终风险等级**:
| 风险值 | 等级 |
|--------|------|
| ≥ 100 | 高风险 |
| 80-100 | 较高风险 |
| 60-80 | 一般风险 |
| < 60 | 低风险 |

---

### 3.6 对比分析模块 (`src/compare/`)

**用途**: 对比相邻两个评估周期的风险变化，识别风险演进趋势。

**工作流程**:
1. 从 MySQL 导出各期评估结果到临时 Excel
2. 按 结构点/路段/路网 三类分别对比
3. 应用风险变化标签规则:

| 标签 | 判定条件 |
|------|----------|
| **风险上行** | 当期和往期均在 [70, 80] 且风险值上升 |
| **当期高风险** | 往期 < 80 且当期 ≥ 80 |
| **持续高风险** | 两期均 ≥ 90 |
| **无需重点关注** | 其他情况 |

4. 输出格式化的对比 Excel 文件，带颜色标注
5. 结果回写 MySQL `risk_contrast` 表

---

## 四、数据流转图

```
┌──────────┐    ┌──────────────────┐    ┌──────────────────┐
│ MySQL DB │───▶│ Input Module     │───▶│ Point Risk       │
│ (输入库)  │    │ 数据导出+ETC合并  │    │ 结构点风险        │
└──────────┘    └──────────────────┘    └───────┬──────────┘
                                                │
                              ┌─────────────────┘
                              │ 输出: 全结构点通行风险值评价表.xlsx
                              │       双月门架风险评估表_路段信息.xlsx
                              ▼
                     ┌──────────────────┐
                     │ Line Risk        │
                     │ 路段风险          │
                     └───────┬──────────┘
                             │
                             │ 输出: 路段通行风险评价总表.xlsx
                             ▼
                    ┌──────────────────┐
                    │ Net Risk         │
                    │ 路网风险          │
                    └───────┬──────────┘
                            │
                            │ 输出: 路网通行风险评估结果_<时间戳>.xlsx
                            ▼
         ┌──────────────────────────────────┐
         │         MySQL (输出库)            │
         │  point_risk_evaluation            │
         │  line_risk_evaluation             │
         │  net_risk_evaluation              │
         └──────────────┬───────────────────┘
                        │
                        ▼
         ┌──────────────────────────────────┐
         │  Compare Module (对比分析)        │
         │  输出: 风险评价对比结果.xlsx       │
         └──────────────────────────────────┘
```

### 关键中间文件

| 文件 | 产生阶段 | 消费阶段 |
|------|---------|---------|
| `全结构点通行风险值评价表.xlsx` | Point Risk | Line Risk |
| `双月门架风险评估表_路段信息.xlsx` | Point Risk | Net Risk |
| `路段通行风险评价总表.xlsx` | Line Risk | Net Risk |

---

## 五、运行方式

### 命令行参数

```bash
# 完整流程: 数据准备 → 点风险 → 线风险 → 网风险
python main.py

# 先同步数据库配置，再执行完整流程
python main.py --config-update

# 完整流程 + 两期对比分析
python main.py --compare

# 跳过风险重算，仅执行数据准备 + 对比分析
python main.py --compare --no-recalculate

# 不执行数据准备 点风险 → 线风险 → 网风险
python main.py --no-input

# 简化日志文件
python main.py --simple-log
```

### 模块独立运行

```bash
# 数据准备 -仅从数据库导入
python -m src.input.input_main --db-only

# 点风险评估
python -m src.point_risk.point_risk_main

# 线风险评估
python -m src.line_risk.line_risk_main

# 网风险评估
python -m src.net_risk.net_risk_main
```

---

## 六、配置文件说明

所有配置文件位于 `config/` 目录，均为标准 INI 格式。

| 文件 | 用途 | 关键参数 |
|------|------|---------|
| `point_risk.ini` | 点风险评估 | 风险等级分值(83/72/55/48)，气象系数(1.00/1.05/1.08/1.12)，交通风险阈值 |
| `line_risk.ini` | 路段风险评估 | 结构物满分(132.82)，结冰/大雾月份，路段名称映射，事故评分标准 |
| `net_risk.ini` | 路网风险评估 | 22条道路属性(长度/设计通行能力/公司)，路网拓扑，参考密度(5.12) |
| `input_db.ini` | 输入数据库连接 | 主机、端口、账号密码、数据库名、belong_date |
| `output_db.ini` | 输出数据库连接 | 主机、端口、账号密码、目标表名 |
| `compare.ini` | 对比分析参数 | 计分列名、对比周期对、阈值(上行/中危/高危) |

可通过 `--config-update` 从 MySQL `freeway_risk_config` 库自动同步最新配置。

---

## 七、输入输出数据格式

### 7.1 输入数据

| 数据项 | 格式 | 来源 |
|--------|------|------|
| 结构物监测基础信息表 | `.xlsx` | `data/input/` |
| 门架信息 | `.xlsx` | `data/input/` |
| 路段评价模板 | `.xlsx` | `data/input/` |
| 气象预警（点位级） | `.json` | `data/input/weather_warnings/` (数组，每项含 datetime/lon/lat/warning_type/warning_level 等) |
| 气象预警（区县级） | `.xlsx` | `data/input/气象预警.xlsx` |
| 交通事故 | `.xlsx` | `data/input/交通事故.xlsx` |
| ETC 门架交通流 | `.csv` | `data/input/traffic_data/` (字段: 门架ID, 车辆ID, 车型, 交易时间, 速度) |
| 路网公司信息 | `.xlsx` | `data/input/路网及其对应公司.xlsx` |

### 7.2 输出数据

| 文件 | 内容 | 路径 |
|------|------|------|
| `全结构点通行风险值评价表.xlsx` | 结构点风险明细 | `data/output/` |
| `路段通行风险评价总表.xlsx` | 路段风险明细 | `data/output/` |
| `路网通行风险评估结果_<时间戳>.xlsx` | 路网风险明细 | `data/output/` |
| `风险评价对比结果.xlsx` | 跨周期对比 | `data/output/` |

### 7.3 MySQL 输出表

| 表名 | 数据库 | 内容 |
|------|--------|------|
| `point_alert_statistic` | `freeway_risk_test` | 结构点气象预警统计 |
| `point_etc_traffic_evaluation` | `freeway_risk_test` | 结构点 ETC 交通评估 |
| `point_risk_evaluation` | `freeway_risk_test` | 结构点综合风险评估 |
| `line_risk_evaluation` | `freeway_risk_test` | 路段综合风险评估 |
| `net_risk_evaluation` | `freeway_risk_test` | 路网综合风险评估 |
| `risk_contrast` | `freeway_risk_test` | 跨周期对比结果 |

---

## 八、关键算法

### 8.1 Haversine 距离计算
用于判断气象预警点是否在结构点的 5km 影响半径内：
```
d = 2 × 6371 × arcsin(√(sin²(Δlat/2) + cos(lat1) × cos(lat2) × sin²(Δlon/2)))
```

### 8.2 加权路段风险 (Net Risk)
```
R = Σ(路段风险值 × 路段流量) / Σ(路段流量)
```

### 8.3 路网连通度风险
```
C = 100 × (1 - C'/2)
其中 C' = 相邻路段数 / 节点数
```

### 8.4 路网密度风险
```
B = (参考密度 - 实际密度) / 参考密度 × 100
实际密度 = 总里程 / (区域面积/100)
参考密度 = 5.12
```

### 8.5 饱和度（加权平均）
```
S = Σ(高峰小时流量 × 路段长度) / Σ(设计通行能力 × 路段长度)
```

### 8.6 均衡性系数
```
E = 1 - σ / X̄  (变异系数的反向指标，值越接近1表示各路段的饱和度越均衡)
```

### 8.7 风险归因贡献度
```
某因子贡献度 = 该因子值 / 最终风险值 × 100%
```

---

## 九、项目目录结构

```
Highway_Risk_Assessment/
├── main.py                                      # 总控入口
├── compare.ini                                  # (旧版) 对比分析配置
├── input_db.ini                                 # (旧版) 输入数据库配置
├── output_db.ini                                # (旧版) 输出数据库配置
├── config/                                      # 配置文件目录
│   ├── point_risk.ini                           #   点风险评估配置
│   ├── line_risk.ini                            #   路段风险评估配置
│   ├── net_risk.ini                             #   路网风险评估配置
│   ├── input_db.ini                             #   输入数据库配置
│   ├── output_db.ini                            #   输出数据库配置
│   └── compare.ini                              #   对比分析配置
├── data/
│   ├── input/                                   # 输入数据文件
│   │   ├── 结构物监测基础信息表(1019)带门架方向.xlsx
│   │   ├── 东南东北渝东门架信息(1).xlsx
│   │   ├── 路段评价模板.xlsx
│   │   ├── 路段通行风险评价总表.xlsx            #   (线→网中间文件)
│   │   ├── 全结构点通行风险值评价表.xlsx        #   (点→线中间文件)
│   │   ├── 双月门架风险评估表_路段信息.xlsx      #   (点→网中间文件)
│   │   ├── 路网及其对应公司.xlsx
│   │   ├── 气象预警.xlsx
│   │   ├── 交通事故.xlsx
│   │   ├── weather_warnings/                    #   气象预警JSON(点位级)
│   │   │   ├── point_alert_25_12_26_1.json
│   │   │   └── point_alert_26_2_26_3.json
│   │   └── traffic_data/                        #   ETC交通流CSV
│   │       └── 双月门架数据全量合并.csv
│   ├── output/                                  # 输出结果文件
│   │   ├── 全结构点通行风险值评价表.xlsx
│   │   ├── 路段通行风险评价总表.xlsx
│   │   ├── 路网通行风险评估结果_<时间戳>.xlsx
│   │   └── 风险评价对比结果.xlsx
│   └── temp/                                    # 中间过程文件
│       ├── 结构点-基础风险值-动态风险值表.xlsx
│       ├── 结构点-基础风险值-动态风险值表_更新.xlsx
│       ├── 结构点-基础风险值-动态风险值表_更新2.xlsx
│       ├── new结构点预警天数统计.xlsx
│       ├── 双月门架风险评估表.xlsx
│       ├── 双月门架风险评估表_路段信息.xlsx
│       └── compare/                             #   对比分析中间文件
│           ├── 结构点/
│           │   ├── 25_12_26_1结构点评估表.xlsx
│           │   └── 26_2_26_3结构点评估表.xlsx
│           ├── 路段/
│           │   ├── 25_12_26_1路段评估表.xlsx
│           │   └── 26_2_26_3路段评估表.xlsx
│           └── 路网/
│               ├── 25_12_26_1路网评估表.xlsx
│               └── 26_2_26_3路网评估表.xlsx
├── src/                                         # 源代码
│   ├── __init__.py
│   ├── db_config.py                             #   统一数据库配置读取
│   ├── input/                                   #   输入数据准备模块
│   │   ├── __init__.py
│   │   ├── input_main.py                        #     模块入口
│   │   ├── config_manager.py                    #     输入配置管理器
│   │   ├── database_exporter.py                 #     MySQL→文件导出
│   │   └── excel_tocsv.py                       #     ETC Excel→CSV合并
│   ├── point_risk/                              #   点风险评估模块
│   │   ├── __init__.py
│   │   ├── point_risk_main.py                   #     独立入口
│   │   ├── config/
│   │   │   ├── __init__.py
│   │   │   └── config_manager.py                #     点风险配置管理器
│   │   ├── data_processing/
│   │   │   ├── __init__.py
│   │   │   ├── base_processor.py                #     抽象基类(Haversine等)
│   │   │   ├── structure_processor.py           #     结构物→基础风险值
│   │   │   ├── weather_processor.py             #     气象预警→动态风险
│   │   │   ├── traffic_processor.py             #     ETC交通→动态风险
│   │   │   └── roadname_processor.py            #     门架→路段名称标定
│   │   ├── database/
│   │   │   ├── __init__.py
│   │   │   └── database_connector.py            #     MySQL输出写入
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   └── data_models.py                   #     数据模型(dataclass)
│   │   ├── risk_calculation/
│   │   │   ├── __init__.py
│   │   │   └── risk_calculator.py               #     风险计算与等级划分
│   │   └── workflow/
│   │       ├── __init__.py
│   │       └── risk_assessment_workflow.py      #     五步工作流编排
│   ├── line_risk/                               #   线(路段)风险评估模块
│   │   ├── __init__.py
│   │   ├── line_risk_main.py                    #     独立入口
│   │   ├── config/
│   │   │   ├── __init__.py
│   │   │   └── config_manager.py                #     线风险配置管理器
│   │   ├── data_processing/
│   │   │   ├── __init__.py
│   │   │   ├── base_processor.py                #     抽象基类
│   │   │   ├── base_risk_processor.py           #     静态风险计算
│   │   │   ├── dynamic_risk_processor.py        #     拥堵/气象动态风险
│   │   │   └── extra_risk_processor.py          #     事故+旅游公路附加风险
│   │   ├── database/
│   │   │   ├── __init__.py
│   │   │   └── database_connector.py            #     MySQL输出写入
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   └── line_models.py                   #     数据模型
│   │   ├── risk_calculation/
│   │   │   ├── __init__.py
│   │   │   └── risk_calculator.py               #     风险等级/归因判定
│   │   └── workflow/
│   │       ├── __init__.py
│   │       └── line_risk_workflow.py            #     六阶段工作流编排
│   ├── net_risk/                                #   网(路网)风险评估模块
│   │   ├── __init__.py
│   │   ├── net_risk_main.py                     #     独立入口
│   │   ├── config/
│   │   │   ├── __init__.py
│   │   │   └── config_manager.py                #     网风险配置管理器
│   │   ├── data_processing/
│   │   │   ├── __init__.py
│   │   │   ├── base_processor.py                #     抽象基类(路名标准化)
│   │   │   ├── traffic_flow_processor.py        #     交通流量处理
│   │   │   ├── road_risk_processor.py           #     路段风险汇总
│   │   │   ├── event_data_processor.py          #     事件数据处理
│   │   │   └── data_merger.py                   #     流量+风险+基础+路由合并
│   │   ├── database/
│   │   │   ├── __init__.py
│   │   │   ├── database_connector.py            #     MySQL连接
│   │   │   ├── database_manager.py              #     数据库管理(建库/建表)
│   │   │   └── network_risk_data_writer.py      #     评估结果写入
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   └── data_models.py                   #     数据模型
│   │   ├── output/
│   │   │   ├── __init__.py
│   │   │   └── excel_writer.py                  #     Excel输出
│   │   ├── risk_calculation/
│   │   │   ├── __init__.py
│   │   │   ├── basic_risk_calculator.py         #     R+C+B→F1+F2+F3
│   │   │   ├── dynamic_coefficient_calculator.py #    饱和度×均衡性系数
│   │   │   ├── additional_coefficient_calculator.py # 到达率×恢复率系数
│   │   │   ├── network_risk_calculator.py       #     最终风险计算
│   │   │   └── risk_attribution_analyzer.py     #     贡献度百分比分析
│   │   └── workflow/
│   │       ├── __init__.py
│   │       └── network_risk_assessment_workflow.py  # 九步工作流编排
│   ├── compare/                                 #   对比分析模块
│   │   ├── __init__.py
│   │   ├── compare.py                           #     核心对比逻辑
│   │   ├── label_input.py                       #     MySQL→临时Excel导出
│   │   └── read_company.py                      #     公司名称映射查询
│   └── config_update/                           #   配置同步模块
│       ├── __init__.py
│       ├── config_updater.py                    #     MySQL→ini 配置同步
│       └── db_table.txt                         #     config库表结构SQL
└── tests/                                       # 测试文件
    ├── __init__.py
    ├── net_tests/
    │   └── test_config_manager.py               #   路网配置管理器测试
    └── point_tests/
        ├── test_weather_processor.py             #   气象处理器测试
        ├── test_traffic.py                       #   交通处理器测试
        ├── test_mapping.py                       #   综合等级映射测试
        ├── verify_mapping.py                     #   映射验证
        ├── verify_new_mapping.py                 #   新映射验证
        ├── verify_weather_processor.py           #   气象处理器验证
        ├── check_columns.py                      #   列名检查
        ├── check_excel_columns.py                #   Excel列检查
        └── check_excel_columns_simple.py         #   Excel列检查(简化版)
```

---

## 十、运行环境

### 10.1 Python 版本

Python 3.11+（项目实际运行于 Python 3.12）

### 10.2 第三方依赖库

| 库 | 用途 | 使用模块 |
|----|------|---------|
| `pandas` | 数据处理与分析（DataFrame 操作、聚合、合并） | 全部模块 |
| `numpy` | 数值计算（数组运算、均值、标准差） | net_risk, point_risk, line_risk |
| `pymysql` | MySQL 数据库连接与操作 | input, point_risk, line_risk, net_risk, compare, config_update |
| `openpyxl` | Excel (.xlsx) 读写与样式设置 | point_risk, line_risk, net_risk, compare |
| `python_calamine` | 大型 Excel 文件快速读取（比 openpyxl 快数倍） | point_risk, line_risk, net_risk |
| `tqdm` | 循环进度条显示 | point_risk, net_risk |
| `chardet` | 自动检测文件编码 | point_risk, net_risk |
| `numba` (可选) | JIT 编译加速 Haversine 距离/交通数据批量运算 | point_risk (traffic_processor.py) |

### 10.3 Python 标准库

| 库 | 用途 | 使用模块 |
|----|------|---------|
| `abc` | 抽象基类定义 | point_risk (base_processor.py) |
| `argparse` | 命令行参数解析 | main.py |
| `calendar` | 月份天数计算 | line_risk |
| `collections` | defaultdict | line_risk, net_risk |
| `configparser` | INI 配置文件读写 | 全部模块 |
| `dataclasses` | 数据类定义 | point_risk, line_risk, net_risk |
| `datetime` | 日期时间处理 | main.py, 全部模块 |
| `gc` | 垃圾回收（大内存操作后手动触发） | point_risk (traffic_processor.py) |
| `glob` | 文件通配符匹配搜索 | input, point_risk, net_risk |
| `hashlib` | MD5 哈希（数据去重标识） | point_risk, net_risk |
| `json` | JSON 文件读写（气象预警数据） | input, point_risk |
| `math` | 数学函数（三角函数、sqrt 等） | point_risk (Haversine) |
| `os` | 文件系统操作 | 全部模块 |
| `pathlib` | 面向对象路径操作 | main.py, 全部模块 |
| `re` | 正则表达式 | net_risk (compare.py) |
| `shutil` | 文件复制 | main.py |
| `sys` | 系统参数、退出码 | main.py, 全部模块 |
| `time` | 计时与延时 | main.py |
| `typing` | 类型注解（List, Dict, Optional 等） | 全部模块 |
| `uuid` | 唯一标识生成 | net_risk (database_manager.py) |
| `warnings` | 警告信息控制 | point_risk, net_risk |

### 10.4 外部依赖

- **MySQL 数据库** (3 个库):
  - `freeway_risk_input` — 输入数据源（结构物监测基础信息、气象预警、交通事故）
  - `freeway_risk` — 输出结果存储（point/line/net 评估结果表、risk_contrast 对比表）
  - `freeway_risk_config` — 配置管理中心（6 张表对应 6 个 INI 文件，支持 `--config-update` 同步）
- **ETC 原始数据**: `../ETC_Data/` 目录下的双月 Excel 文件（上游系统产出）

### 10.5 安装命令

```bash
pip install pandas numpy pymysql openpyxl python-calamine tqdm chardet numba
```

> `numba` 为可选依赖，未安装时系统自动回退到纯 Python 计算路径，影响大型数据处理性能。

---

## 十一、测试

测试代码位于 `tests/` 目录：

| 文件 | 测试内容 | 运行方式 |
|------|---------|---------|
| `tests/point_tests/test_weather_processor.py` | 气象预警天数统计与动态风险系数 | `python tests/point_tests/test_weather_processor.py` |
| `tests/point_tests/test_traffic.py` | 交通数据加载与门架风险评估 | `python tests/point_tests/test_traffic.py` |
| `tests/point_tests/test_mapping.py` | 综合等级→基础风险值映射 | `python tests/point_tests/test_mapping.py` |
| `tests/point_tests/verify_mapping.py` | 映射结果数据验证 | `python tests/point_tests/verify_mapping.py` |
| `tests/point_tests/verify_new_mapping.py` | 新版映射数据验证 | `python tests/point_tests/verify_new_mapping.py` |
| `tests/point_tests/verify_weather_processor.py` | 气象处理器输出验证 | `python tests/point_tests/verify_weather_processor.py` |
| `tests/point_tests/check_columns.py` | 输入数据列名检查 | `python tests/point_tests/check_columns.py` |
| `tests/point_tests/check_excel_columns.py` | Excel 文件列名检查 | `python tests/point_tests/check_excel_columns.py` |
| `tests/point_tests/check_excel_columns_simple.py` | Excel 列名检查(简化版) | `python tests/point_tests/check_excel_columns_simple.py` |
| `tests/net_tests/test_config_manager.py` | 路网配置管理器（INI 读写） | `python tests/net_tests/test_config_manager.py` |

也可以使用 pytest 批量运行：
```bash
pip install pytest
python -m pytest tests/ -v
```
